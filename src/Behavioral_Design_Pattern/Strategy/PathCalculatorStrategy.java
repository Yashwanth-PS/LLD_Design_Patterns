package Behavioral_Design_Pattern.Strategy;

public interface PathCalculatorStrategy { // Step1: Create a Strategy Interface with an Abstract Method
    void findPath(String source, String destination);
}
